#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = Array.new(9, " ")
puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"
display_board(board)

puts "Please Enter 1-9:"
their_move = gets.strip
index = their_move.to_i-1
input_to_index(input)
move(board, index, value)
